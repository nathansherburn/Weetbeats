{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the Weetbeats window is allowed to do. The folder picker is opened from Rust, so the front end needs nothing beyond the core defaults.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
