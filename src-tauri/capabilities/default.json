{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the Weetbeats window is allowed to do. The pickers and the menu are driven from Rust, so the only thing the front end needs beyond the core defaults is permission to drag the window by its own header — which the core defaults leave out.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
