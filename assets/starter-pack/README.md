# Starter pack

Eight drums, synthesised from scratch by `tools/starter-pack`. Not sampled from anything,
so there is no licence to honour and nobody to credit. Same terms as the rest of the
project: MIT, do what you like.

Regenerate with `cargo run -p starter-pack -- assets/starter-pack`. The output is
deterministic, so nothing changes unless the generator does.
